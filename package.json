{
  "name": "unne-puebla-redesign",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "check": "astro check",
    "validate:production": "node scripts/validate-production.mjs",
    "build": "npm run validate:production && astro build",
    "preview": "astro preview",
    "test:links": "node scripts/check-links.mjs",
    "test:visual": "node scripts/visual-check.cjs"
  },
  "dependencies": {
    "@fontsource-variable/manrope": "^5.1.0",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "typescript": "^5.7.0"
  }
}
